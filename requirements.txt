# Core
torch
transformers
accelerate
sentencepiece
jsonlines
pandas
tqdm
numpy

# GLiNER
gliner

# GENRE (HuggingFace transformers version — no fairseq needed)
# facebook/genre-linking-aidayago2 loaded via transformers

# Jupyter
jupyter
ipywidgets